{{#if footerControls}}
	<footer>
		<ul>
			{{#each footerControls}}
			{{!-- spreadClass: string[] => string string string --}}
				<li class="{{spreadClasses this.classes}}">
					{{this.text}}
				</li>
			{{/each}}
		</ul>
	</footer>
{{/if}}